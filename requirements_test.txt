# Dev/test-only dependency - never referenced by manifest.json or shipped with the integration.
# `pip install -r requirements_test.txt` before running pytest at all (tests/conftest.py
# registers the plugin for the whole suite - see CLAUDE.md's Verification section).
pytest-homeassistant-custom-component

# Used by two separate `mypy --strict` scopes (own CI job each, see .github/workflows/test.yml):
# podpoint-mobile-api/src/podpoint_mobile_api/ (the standalone package) and
# custom_components/pod_home/ (the integration itself, excluding its vendored copy of the
# package above).
mypy
